// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
 
}
model User {
  id String @id @default(uuid())
  name String
  email String @unique
  password String

  createdAt DateTime @default(now())
}
model Song{
  id String @id @default(uuid())
  title String
  runtime Int
  genres String[] @default([])
  createdBy String
  createdAt DateTime @default(now())
}
model FavouriteSong{
  id  String @id @default(uuid())
  userId String
  songId String
  createdAt DateTime @default(now())

  favUser User @relation(fields:[userid], references:[id] ,onDelete:Cascade)
  favsong Song @relation(fields:[songId], references:[id] ,onDelete:Cascade)
}
model Recentlyplayed{
  id String @id @default(uuid())
 userId String
  songId String
  playedAt DateTime @default(now())

  RecPlayed User @relation(fields:[userId], references:[id],onDelete:Cascade)
  RecSong Song @relation (fields:[songId],references:[id],onDelete:Cascade)

  }
  model playlist{
    id String @id @default(uuid())
    userId String
    songId String
    // relations
    user User @relation(fields:[userId], references:[id], onDelete:Cascade)
    song Song @relation(fields:[songId], references:[id],onDelete:Cascade)
  }